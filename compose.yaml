################################################
##   EAC3 Converter - Convert audio to EAC3   ##
################################################

---
services:
  eac3_converter:
    container_name: eac3_converter
    image: simonverbois/eac3-converter:latest
    volumes:
      - ./path/to/folder1:/app/input/folder1:rw
      - ./path/to/folder2:/app/input/folder2:rw
      - eac3_cache:/app/cache:rw
    environment:
      # System
      TZ: Europe/Paris
      # App
      DEBUG_MODE: "false"
      # Schedule
      START_TIME: "04:00"
      RUN_IMMEDIATELY: "false"
      # FFmpeg - audio quality (dynamic bitrate based on channel count)
      FFMPEG_BITRATE_STEREO: 384k
      FFMPEG_BITRATE_SURROUND: 1536k
      FFMPEG_BITRATE_SURROUND_PLUS: 1664k
      FFMPEG_DIALNORM: "-27"
      FFMPEG_MIXING_LEVEL: "80"
      # FFmpeg - other
      FFMPEG_STRICT_MODE: "-2"
      FFMPEG_FLAGS: "+genpts"
      FFMPEG_TIMEOUT_SECONDS: "3600"
      FFMPEG_MIN_DISK_SPACE_RATIO: "1.5"
      FFMPEG_THREADS: "0"
      FFMPEG_BUFSIZE: 128k
      FFMPEG_PERFORMANCE_FLAGS: "+discardcorrupt+genpts+igndts+ignidx"
      FFMPEG_AVOID_NEGATIVE_TS: make_zero
      FFMPEG_MAX_MUXING_QUEUE_SIZE: "1024"
      # Standalone audio files (e.g. external .dts loaded by Jellyfin)
      PROCESS_STANDALONE_AUDIO: "false"
      STANDALONE_AUDIO_EXTENSIONS: "dts,thd,truehd,dtshd"
      STANDALONE_AUDIO_KEEP_ORIGINAL: "false"
      STANDALONE_AUDIO_OUTPUT_EXTENSION: "ec3"
    restart: no # Change this setting if you run with the scheduler
    network_mode: none
    deploy:
      resources:
        limits:
          cpus: '4.0'
          memory: '2G'
        reservations:
          cpus: '1.0'
          memory: '512M'
    healthcheck:
      test: ["CMD", "pgrep", "-f", "src.main"]
      interval: 1m
      timeout: 10s
      retries: 3

volumes:
  eac3_cache:
